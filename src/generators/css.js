/**
 * Generate the component css
 * @param   { String } source - css source
 * @param   { Object } options - options needed to generate the css
 * @returns { Promise } - async string output
 */
export default function css(source, options) {
  return new Promise(resolve => {
    return resolve(source)
  })
}